# Service for the application
apiVersion: v1
kind: Service
metadata:
  name: my-web-app
  labels:
    app: rishi-web-app
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: rishi-web-app
  type: ClusterIP
